import React from "react";

const MapFrame = ({ src, width, height }) => (
  // <iframe src={src} width={width} height={height} scrolling="no"></iframe>
  <iframe src={src} width={width} height={height} scrolling="no"></iframe>
);

export default MapFrame;
